# Use the random library's choice() method to get a random item from
# the list of names below, and store the chosen name in a variable called
# raffle.
import random

names = ["Samantha", "Carrie", "Chris", "Charlotte", "Richard"]
raffle = random.choice(names)

print(raffle)


